{
  "name": "flamingo-ramen",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 4201 --strictPort",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4201 --strictPort",
    "test": "node --test tests/*.test.js",
    "capture": "node scripts/capture.mjs"
  },
  "devDependencies": {
    "@napi-rs/canvas": "0.1.80",
    "galmuri": "^2.40.3",
    "vite": "8.2.2"
  }
}
